#nameless function k lamda function bole.
#lambda function is a small anonymous function which can take any number of arguments but can only have on

x = lambda a,b,c,d: (a + b) * c / d            #use lambda keyword. no need 1st brackets.
print(x(99,165,2,10))

#or we can use lambda keyword in any function parameter.
def function(lambda)
    return lambda

print(function(lambda a,b: a + b (99,165)))
